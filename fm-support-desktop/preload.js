// Intentionally empty: the app is just the hosted web app rendered in a
// native window, so no privileged bridge between Node and the page is
// needed. Kept as a placeholder in case native-only features (e.g. desktop
// notifications, auto-update) are added later.
